name: Changelog

on:
  push:
    tags:
      - "v*"

jobs:
  tagged-release:
    name: Changelog
    runs-on: ubuntu-latest
    # needs: release

    permissions:
      contents: write

    steps:
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
