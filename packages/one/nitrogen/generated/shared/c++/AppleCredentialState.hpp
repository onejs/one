///
/// AppleCredentialState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (AppleCredentialState).
   */
  enum class AppleCredentialState {
    REVOKED      SWIFT_NAME(revoked) = 0,
    AUTHORIZED      SWIFT_NAME(authorized) = 1,
    NOTFOUND      SWIFT_NAME(notfound) = 2,
    TRANSFERRED      SWIFT_NAME(transferred) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ AppleCredentialState <> JS AppleCredentialState (union)
  template <>
  struct JSIConverter<margelo::nitro::one::AppleCredentialState> final {
    static inline margelo::nitro::one::AppleCredentialState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("revoked"): return margelo::nitro::one::AppleCredentialState::REVOKED;
        case hashString("authorized"): return margelo::nitro::one::AppleCredentialState::AUTHORIZED;
        case hashString("notFound"): return margelo::nitro::one::AppleCredentialState::NOTFOUND;
        case hashString("transferred"): return margelo::nitro::one::AppleCredentialState::TRANSFERRED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum AppleCredentialState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::AppleCredentialState arg) {
      switch (arg) {
        case margelo::nitro::one::AppleCredentialState::REVOKED: return JSIConverter<std::string>::toJSI(runtime, "revoked");
        case margelo::nitro::one::AppleCredentialState::AUTHORIZED: return JSIConverter<std::string>::toJSI(runtime, "authorized");
        case margelo::nitro::one::AppleCredentialState::NOTFOUND: return JSIConverter<std::string>::toJSI(runtime, "notFound");
        case margelo::nitro::one::AppleCredentialState::TRANSFERRED: return JSIConverter<std::string>::toJSI(runtime, "transferred");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert AppleCredentialState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("revoked"):
        case hashString("authorized"):
        case hashString("notFound"):
        case hashString("transferred"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
