///
/// AppleAuthScope.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (AppleAuthScope).
   */
  enum class AppleAuthScope {
    FULLNAME      SWIFT_NAME(fullname) = 0,
    EMAIL      SWIFT_NAME(email) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ AppleAuthScope <> JS AppleAuthScope (union)
  template <>
  struct JSIConverter<margelo::nitro::one::AppleAuthScope> final {
    static inline margelo::nitro::one::AppleAuthScope fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("fullName"): return margelo::nitro::one::AppleAuthScope::FULLNAME;
        case hashString("email"): return margelo::nitro::one::AppleAuthScope::EMAIL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum AppleAuthScope - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::AppleAuthScope arg) {
      switch (arg) {
        case margelo::nitro::one::AppleAuthScope::FULLNAME: return JSIConverter<std::string>::toJSI(runtime, "fullName");
        case margelo::nitro::one::AppleAuthScope::EMAIL: return JSIConverter<std::string>::toJSI(runtime, "email");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert AppleAuthScope to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("fullName"):
        case hashString("email"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
