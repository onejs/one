///
/// SpeechEventType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (SpeechEventType).
   */
  enum class SpeechEventType {
    ERROR      SWIFT_NAME(error) = 0,
    START      SWIFT_NAME(start) = 1,
    TRANSCRIPT      SWIFT_NAME(transcript) = 2,
    END      SWIFT_NAME(end) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ SpeechEventType <> JS SpeechEventType (union)
  template <>
  struct JSIConverter<margelo::nitro::one::SpeechEventType> final {
    static inline margelo::nitro::one::SpeechEventType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("error"): return margelo::nitro::one::SpeechEventType::ERROR;
        case hashString("start"): return margelo::nitro::one::SpeechEventType::START;
        case hashString("transcript"): return margelo::nitro::one::SpeechEventType::TRANSCRIPT;
        case hashString("end"): return margelo::nitro::one::SpeechEventType::END;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SpeechEventType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::SpeechEventType arg) {
      switch (arg) {
        case margelo::nitro::one::SpeechEventType::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        case margelo::nitro::one::SpeechEventType::START: return JSIConverter<std::string>::toJSI(runtime, "start");
        case margelo::nitro::one::SpeechEventType::TRANSCRIPT: return JSIConverter<std::string>::toJSI(runtime, "transcript");
        case margelo::nitro::one::SpeechEventType::END: return JSIConverter<std::string>::toJSI(runtime, "end");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SpeechEventType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("error"):
        case hashString("start"):
        case hashString("transcript"):
        case hashString("end"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
