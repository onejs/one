///
/// LocalBiometryType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (LocalBiometryType).
   */
  enum class LocalBiometryType {
    NONE      SWIFT_NAME(none) = 0,
    TOUCHID      SWIFT_NAME(touchid) = 1,
    FACEID      SWIFT_NAME(faceid) = 2,
    OPTICID      SWIFT_NAME(opticid) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ LocalBiometryType <> JS LocalBiometryType (union)
  template <>
  struct JSIConverter<margelo::nitro::one::LocalBiometryType> final {
    static inline margelo::nitro::one::LocalBiometryType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return margelo::nitro::one::LocalBiometryType::NONE;
        case hashString("touchID"): return margelo::nitro::one::LocalBiometryType::TOUCHID;
        case hashString("faceID"): return margelo::nitro::one::LocalBiometryType::FACEID;
        case hashString("opticID"): return margelo::nitro::one::LocalBiometryType::OPTICID;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum LocalBiometryType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::LocalBiometryType arg) {
      switch (arg) {
        case margelo::nitro::one::LocalBiometryType::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case margelo::nitro::one::LocalBiometryType::TOUCHID: return JSIConverter<std::string>::toJSI(runtime, "touchID");
        case margelo::nitro::one::LocalBiometryType::FACEID: return JSIConverter<std::string>::toJSI(runtime, "faceID");
        case margelo::nitro::one::LocalBiometryType::OPTICID: return JSIConverter<std::string>::toJSI(runtime, "opticID");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert LocalBiometryType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("touchID"):
        case hashString("faceID"):
        case hashString("opticID"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
