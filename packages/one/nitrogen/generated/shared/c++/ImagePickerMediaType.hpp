///
/// ImagePickerMediaType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (ImagePickerMediaType).
   */
  enum class ImagePickerMediaType {
    IMAGES      SWIFT_NAME(images) = 0,
    VIDEOS      SWIFT_NAME(videos) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ ImagePickerMediaType <> JS ImagePickerMediaType (union)
  template <>
  struct JSIConverter<margelo::nitro::one::ImagePickerMediaType> final {
    static inline margelo::nitro::one::ImagePickerMediaType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("images"): return margelo::nitro::one::ImagePickerMediaType::IMAGES;
        case hashString("videos"): return margelo::nitro::one::ImagePickerMediaType::VIDEOS;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ImagePickerMediaType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::ImagePickerMediaType arg) {
      switch (arg) {
        case margelo::nitro::one::ImagePickerMediaType::IMAGES: return JSIConverter<std::string>::toJSI(runtime, "images");
        case margelo::nitro::one::ImagePickerMediaType::VIDEOS: return JSIConverter<std::string>::toJSI(runtime, "videos");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ImagePickerMediaType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("images"):
        case hashString("videos"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
