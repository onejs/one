///
/// HingeStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (HingeStatus).
   */
  enum class HingeStatus {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    CLOSED      SWIFT_NAME(closed) = 1,
    PARTIALLYOPEN      SWIFT_NAME(partiallyopen) = 2,
    FULLYOPEN      SWIFT_NAME(fullyopen) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ HingeStatus <> JS HingeStatus (union)
  template <>
  struct JSIConverter<margelo::nitro::one::HingeStatus> final {
    static inline margelo::nitro::one::HingeStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"): return margelo::nitro::one::HingeStatus::UNKNOWN;
        case hashString("closed"): return margelo::nitro::one::HingeStatus::CLOSED;
        case hashString("partiallyOpen"): return margelo::nitro::one::HingeStatus::PARTIALLYOPEN;
        case hashString("fullyOpen"): return margelo::nitro::one::HingeStatus::FULLYOPEN;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum HingeStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::HingeStatus arg) {
      switch (arg) {
        case margelo::nitro::one::HingeStatus::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::one::HingeStatus::CLOSED: return JSIConverter<std::string>::toJSI(runtime, "closed");
        case margelo::nitro::one::HingeStatus::PARTIALLYOPEN: return JSIConverter<std::string>::toJSI(runtime, "partiallyOpen");
        case margelo::nitro::one::HingeStatus::FULLYOPEN: return JSIConverter<std::string>::toJSI(runtime, "fullyOpen");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert HingeStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"):
        case hashString("closed"):
        case hashString("partiallyOpen"):
        case hashString("fullyOpen"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
