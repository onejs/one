// web stub - a `.swift` import on web is served by the browser simulator, never this
export type { SwiftPackageViewProps } from './types'

import type { SwiftPackageViewProps } from './types'

export function SwiftPackageView(_props: SwiftPackageViewProps) {
  return null
}
