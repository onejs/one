// generated by bun run generate from the SwiftUI SDK and codegen/catalog.ts.
// edit the generator or catalog, then regenerate.
import { Platform } from 'react-native'
import { useControlled } from '../controlled'
import { assertSwiftUIValue } from './swiftui'
import type * as Types from './controlTypes'
import NativePicker from '../specs/OneNativePickerNativeComponent'
export function Picker({
  selection,
  onSelectionChange,
  revision = 0,
  label = '',
  disabled = false,
  options,
  pickerStyle = 'automatic',
  style,
  ...props
}: Types.PickerProps) {
  if (
    !Array.isArray(options) ||
    options.some(
      (option) => typeof option?.value !== 'string' || typeof option?.label !== 'string'
    )
  )
    throw new Error('Picker options must contain string value and label fields')
  if (!options.length) throw new Error('Picker options must not be empty')
  if (new Set(options.map((option) => option.value)).size !== options.length)
    throw new Error('Picker option values must be unique')
  if (!options.some((option) => option.value === selection))
    throw new Error('Picker selection must match an option value')
  if (pickerStyle === 'navigationLink' || pickerStyle === 'palette')
    throw new Error(
      'PickerStyle.' +
        pickerStyle +
        ' requires a native container context that One Native does not provide yet'
    )
  assertSwiftUIValue(
    'PickerStyle',
    pickerStyle,
    Number.parseFloat(String(Platform.Version))
  )
  const controlled = useControlled<{
    value: string
    eventCount: number
    revision: number
  }>((event) => onSelectionChange(event.value), revision)
  return (
    <NativePicker
      {...props}
      style={[
        { height: pickerStyle === 'wheel' || pickerStyle === 'inline' ? 216 : 44 },
        style,
      ]}
      value={selection}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      options={options}
      pickerStyle={pickerStyle}
      onNativePickerValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
import NativeDatePicker from '../specs/OneNativeDatePickerNativeComponent'
export function DatePicker({
  selection,
  onSelectionChange,
  revision = 0,
  label = '',
  disabled = false,
  minimumDate = new Date(-62135769600000),
  maximumDate = new Date(64092211200000),
  displayedComponents = 'dateAndTime',
  datePickerStyle = 'automatic',
  style,
  ...props
}: Types.DatePickerProps) {
  const selectionTime = selection instanceof Date ? selection.getTime() : NaN
  const minimumTime = minimumDate instanceof Date ? minimumDate.getTime() : NaN
  const maximumTime = maximumDate instanceof Date ? maximumDate.getTime() : NaN
  if (![selectionTime, minimumTime, maximumTime].every(Number.isFinite))
    throw new Error(
      'DatePicker selection, minimumDate, and maximumDate must be valid Date values'
    )
  if (minimumTime > maximumTime)
    throw new Error('DatePicker minimumDate must not be after maximumDate')
  if (selectionTime < minimumTime || selectionTime > maximumTime)
    throw new Error('DatePicker selection must be within minimumDate and maximumDate')
  if (!['date', 'hourAndMinute', 'dateAndTime'].includes(displayedComponents))
    throw new Error(
      'DatePicker displayedComponents must be date, hourAndMinute, or dateAndTime'
    )
  assertSwiftUIValue(
    'DatePickerStyle',
    datePickerStyle,
    Number.parseFloat(String(Platform.Version))
  )
  const controlled = useControlled<{
    value: number
    eventCount: number
    revision: number
  }>((event) => onSelectionChange(new Date(event.value)), revision)
  return (
    <NativeDatePicker
      {...props}
      style={[
        {
          height:
            datePickerStyle === 'graphical'
              ? 360
              : datePickerStyle === 'wheel'
                ? 216
                : 44,
        },
        style,
      ]}
      value={selection.getTime()}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      minimumDate={minimumDate.getTime()}
      maximumDate={maximumDate.getTime()}
      displayedComponents={displayedComponents}
      datePickerStyle={datePickerStyle}
      onNativeDatePickerValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
import NativeColorPicker from '../specs/OneNativeColorPickerNativeComponent'
export function ColorPicker({
  selection,
  onSelectionChange,
  revision = 0,
  label = '',
  disabled = false,
  supportsOpacity = true,
  style,
  ...props
}: Types.ColorPickerProps) {
  if (
    typeof selection !== 'string' ||
    !/^#[0-9A-Fa-f]{6}(?:[0-9A-Fa-f]{2})?$/.test(selection)
  )
    throw new Error('ColorPicker selection must be #RRGGBB or #RRGGBBAA')

  const controlled = useControlled<{
    value: string
    eventCount: number
    revision: number
  }>((event) => onSelectionChange(event.value), revision)
  return (
    <NativeColorPicker
      {...props}
      style={[{ height: 44 }, style]}
      value={selection}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      supportsOpacity={supportsOpacity}
      onNativeColorPickerValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
import NativeToggle from '../specs/OneNativeToggleNativeComponent'
export function Toggle({
  isOn,
  onIsOnChange,
  revision = 0,
  label = '',
  disabled = false,
  toggleStyle = 'automatic',
  style,
  ...props
}: Types.ToggleProps) {
  if (typeof isOn !== 'boolean') throw new Error('Toggle isOn must be a boolean')
  assertSwiftUIValue(
    'ToggleStyle',
    toggleStyle,
    Number.parseFloat(String(Platform.Version))
  )
  const controlled = useControlled<{
    value: boolean
    eventCount: number
    revision: number
  }>((event) => onIsOnChange(event.value), revision)
  return (
    <NativeToggle
      {...props}
      style={[{ height: 44 }, style]}
      value={isOn}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      toggleStyle={toggleStyle}
      onNativeToggleValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
import NativeSlider from '../specs/OneNativeSliderNativeComponent'
export function Slider({
  value,
  onValueChange,
  revision = 0,
  label = '',
  disabled = false,
  minimumValue = 0,
  maximumValue = 100,
  step = 1,
  style,
  ...props
}: Types.SliderProps) {
  if (![value, minimumValue, maximumValue, step].every(Number.isFinite))
    throw new Error(
      'Slider value, minimumValue, maximumValue, and step must be finite numbers'
    )
  if (minimumValue >= maximumValue)
    throw new Error('Slider minimumValue must be less than maximumValue')
  if (step <= 0) throw new Error('Slider step must be greater than 0')
  if (value < minimumValue || value > maximumValue)
    throw new Error('Slider value must be within minimumValue and maximumValue')

  const controlled = useControlled<{
    value: number
    eventCount: number
    revision: number
  }>((event) => onValueChange(event.value), revision)
  return (
    <NativeSlider
      {...props}
      style={[{ height: 44 }, style]}
      value={value}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      minimumValue={minimumValue}
      maximumValue={maximumValue}
      step={step}
      onNativeSliderValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
import NativeStepper from '../specs/OneNativeStepperNativeComponent'
export function Stepper({
  value,
  onValueChange,
  revision = 0,
  label = '',
  disabled = false,
  minimumValue = 0,
  maximumValue = 100,
  step = 1,
  style,
  ...props
}: Types.StepperProps) {
  if (![value, minimumValue, maximumValue, step].every(Number.isFinite))
    throw new Error(
      'Stepper value, minimumValue, maximumValue, and step must be finite numbers'
    )
  if (minimumValue >= maximumValue)
    throw new Error('Stepper minimumValue must be less than maximumValue')
  if (step <= 0) throw new Error('Stepper step must be greater than 0')
  if (value < minimumValue || value > maximumValue)
    throw new Error('Stepper value must be within minimumValue and maximumValue')

  const controlled = useControlled<{
    value: number
    eventCount: number
    revision: number
  }>((event) => onValueChange(event.value), revision)
  return (
    <NativeStepper
      {...props}
      style={[{ height: 44 }, style]}
      value={value}
      acknowledgedEvent={controlled.acknowledgedEvent}
      revision={revision}
      label={label}
      disabled={disabled}
      minimumValue={minimumValue}
      maximumValue={maximumValue}
      step={step}
      onNativeStepperValueChange={({ nativeEvent }) =>
        controlled.onNativeChange(nativeEvent)
      }
    />
  )
}
