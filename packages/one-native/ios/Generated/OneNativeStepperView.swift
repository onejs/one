// generated by bun run generate from the SwiftUI SDK and codegen/catalog.ts.
// edit the generator or catalog, then regenerate.
import SwiftUI
import UIKit

private final class StepperModel: ObservableObject {
  @Published var controlled = OneNativeControlled<Double>(0)
  @Published var label: String = ""
  @Published var disabled: Bool = false
  @Published var minimumValue: Double = 0
  @Published var maximumValue: Double = 100
  @Published var step: Double = 1
  var active = false
  var onChange: ((Double, Int, Int) -> Void)?
  func change(_ value: Double) {
    guard active, !disabled, controlled.value != value else { return }
    controlled.change(value)
    onChange?(value, controlled.eventCount, controlled.revision)
  }
}
@objcMembers public final class OneNativeStepperView: UIView, OneNativeComposable {
  public var onChange: ((Double, Int, Int) -> Void)?
  private var model = StepperModel()
  private var controller: OneNativeHostingController<OneNativeStandalone<StepperContent>>?
  public override init(frame: CGRect) { super.init(frame: frame) }
  required init?(coder: NSCoder) { fatalError("init(coder:) is unavailable") }
  public func configure(_ value: Double, acknowledgedEvent: Int, revision: Int, label: String, disabled: Bool, minimumValue: Double, maximumValue: Double, step: Double) {
    if let next = model.controlled.applying(value, acknowledged: acknowledgedEvent, revision: revision) { model.controlled = next }
    if model.label != label { model.label = label }
    if model.disabled != disabled { model.disabled = disabled }
    if model.minimumValue != minimumValue { model.minimumValue = minimumValue }
    if model.maximumValue != maximumValue { model.maximumValue = maximumValue }
    if model.step != step { model.step = step }
  }


  private weak var compositionParent: OneNativeCompositionParent?
  public func compositionContent() -> AnyView { AnyView(StepperContent(model: model)) }
  // composed, there is no window to wait for, so publication is what activates it.
  public func composeInto(_ parent: OneNativeCompositionParent) {
    controller?.detach(); controller = nil
    compositionParent = parent
    bindCallbacks()
    model.active = true
  }
  public func decompose() { compositionParent = nil; model.active = false }
  public override func didMoveToWindow() { super.didMoveToWindow(); updateHost() }
  public override func layoutSubviews() { super.layoutSubviews(); updateHost() }
  private func bindCallbacks() {
    model.onChange = { [weak self] value, count, revision in self?.onChange?(value, count, revision) }
  }
  private func updateHost() {
    guard compositionParent == nil else { return }
    model.active = false
    guard window != nil else { controller?.detach(); return }
    if controller == nil {
      bindCallbacks()
      controller = OneNativeHostingController(rootView: OneNativeStandalone(content: StepperContent(model: model)))
    }
    controller?.attach(to: self)
    model.active = controller?.parent != nil
  }
  public func reset() {
    compositionParent = nil
    model.active = false; model.onChange = nil
    controller?.detach(); controller = nil; model = StepperModel()
  }
}
private struct StepperContent: View {
  @ObservedObject var model: StepperModel
  var body: some View {
    Stepper(value: Binding(
        get: { model.controlled.value },
        set: { value in model.change(value) }
      ), in: model.minimumValue...model.maximumValue, step: model.step) {
        Text(model.label)
      } onEditingChanged: { _ in }
      .disabled(model.disabled)
  }
}
