// generated by bun run generate from the SwiftUI SDK and codegen/catalog.ts.
// edit the generator or catalog, then regenerate.
import SwiftUI
import UIKit

private final class ShareLinkModel: ObservableObject {
  @Published var label: String = ""
  @Published var disabled: Bool = false
  @Published var systemImage: String = ""
  @Published var item: String = ""
  @Published var itemType: String = "text"
  @Published var subject: String = ""
  @Published var message: String = ""
  @Published var accessibility = OneNativeAccessibility()
  @Published var swiftStyle = OneNativeStyle()
  var active = false
}
@objcMembers public final class OneNativeShareLinkView: UIView, OneNativeComposable {
  private var model = ShareLinkModel()
  public var onHeight: ((CGFloat) -> Void)?
  private var controller: OneNativeHostingController<OneNativeMeasuredStandalone<ShareLinkContent>>?
  public override init(frame: CGRect) { super.init(frame: frame) }
  required init?(coder: NSCoder) { fatalError("init(coder:) is unavailable") }
  public func configureAccessibility(_ label: String, hint: String, value: String, identifier: String) {
    let next = OneNativeAccessibility(label: label, hint: hint, value: value, identifier: identifier)
    if model.accessibility != next { model.accessibility = next }
  }
  public func configureStyle(_ style: [String: Any]) {
    let next = OneNativeStyle(dictionary: style)
    if model.swiftStyle != next { model.swiftStyle = next }
  }
  public func configure(_ label: String, disabled: Bool, systemImage: String, item: String, itemType: String, subject: String, message: String) {
    if model.label != label { model.label = label }
    if model.disabled != disabled { model.disabled = disabled }
    if model.systemImage != systemImage { model.systemImage = systemImage }
    if model.item != item { model.item = item }
    if model.itemType != itemType { model.itemType = itemType }
    if model.subject != subject { model.subject = subject }
    if model.message != message { model.message = message }
  }


  private weak var compositionParent: OneNativeCompositionParent?
  public func compositionContent() -> AnyView { AnyView(ShareLinkContent(model: model)) }
  // composed, there is no window to wait for, so publication is what activates it.
  public func composeInto(_ parent: OneNativeCompositionParent) {
    controller?.detach(); controller = nil
    compositionParent = parent
    bindCallbacks()
    model.active = true
  }
  public func decompose() { compositionParent = nil; model.active = false }
  public override func didMoveToWindow() { super.didMoveToWindow(); updateHost() }
  public override func layoutSubviews() { super.layoutSubviews(); updateHost() }
  private func bindCallbacks() {
  }
  private func updateHost() {
    guard compositionParent == nil else { return }
    model.active = false
    guard window != nil else { controller?.detach(); return }
    if controller == nil {
      bindCallbacks()
      controller = OneNativeHostingController(rootView: OneNativeMeasuredStandalone(content: ShareLinkContent(model: model), onHeight: { [weak self] height in self?.onHeight?(height) }))
    }
    controller?.attach(to: self)
    model.active = controller?.parent != nil
  }
  public func reset() {
    compositionParent = nil
    model.active = false
    controller?.detach(); controller = nil; model = ShareLinkModel()
  }
}
private struct ShareLinkContent: View {
  @ObservedObject var model: ShareLinkModel
  var body: some View {
    ShareLinkSurface(model: model)
      .disabled(model.disabled)
      .oneNativeAccessibility(model.accessibility)
      .oneNativeStyle(model.swiftStyle)
  }
}
private struct ShareLinkSurface: View {
  @ObservedObject var model: ShareLinkModel
  // an empty string is no subject and no message, which the SDK spells as nil.
  private var subject: Text? { model.subject.isEmpty ? nil : Text(model.subject) }
  private var message: Text? { model.message.isEmpty ? nil : Text(model.message) }
  @ViewBuilder private var label: some View {
    if model.systemImage.isEmpty { Text(model.label) }
    else { Label(model.label, systemImage: model.systemImage) }
  }
  var body: some View {
    // a url that does not parse falls through to sharing the text, which is what it is.
    if model.itemType == "url", let url = URL(string: model.item) {
      ShareLink(item: url, subject: subject, message: message) { label }
    } else {
      ShareLink(item: model.item, subject: subject, message: message) { label }
    }
  }
}
