// generated by bun run generate from the SwiftUI SDK and codegen/catalog.ts.
// edit the generator or catalog, then regenerate.
import SwiftUI
import UIKit

private final class ContentUnavailableViewModel: ObservableObject {
  @Published var title: String = ""
  @Published var systemImage: String = ""
  @Published var description: String = ""
  @Published var actions: [OneNativeDialogAction] = []
  @Published var accessibility = OneNativeAccessibility()
  @Published var swiftStyle = OneNativeStyle()
  var active = false
  var onAction: ((String, Int) -> Void)?
  private var actionCount = 0
  func action(_ id: String) {
    guard active else { return }
    actionCount += 1
    onAction?(id, actionCount)
  }
}
@objcMembers public final class OneNativeContentUnavailableViewView: UIView, OneNativeComposable {
  public var onAction: ((String, Int) -> Void)?
  private var model = ContentUnavailableViewModel()
  private var controller: OneNativeHostingController<OneNativeStandalone<ContentUnavailableViewContent>>?
  public override init(frame: CGRect) { super.init(frame: frame) }
  required init?(coder: NSCoder) { fatalError("init(coder:) is unavailable") }
  public func configureAccessibility(_ label: String, hint: String, value: String, identifier: String) {
    let next = OneNativeAccessibility(label: label, hint: hint, value: value, identifier: identifier)
    if model.accessibility != next { model.accessibility = next }
  }
  public func configureStyle(_ style: [String: Any]) {
    let next = OneNativeStyle(dictionary: style)
    if model.swiftStyle != next { model.swiftStyle = next }
  }
  public func configure(_ title: String, systemImage: String, description: String) {
    if model.title != title { model.title = title }
    if model.systemImage != systemImage { model.systemImage = systemImage }
    if model.description != description { model.description = description }
  }
  public func setActions(_ items: [[String: Any]]) { model.actions = items.map { OneNativeDialogAction(id: $0["id"] as! String, label: $0["label"] as! String, role: $0["role"] as! String) } }

  private weak var compositionParent: OneNativeCompositionParent?
  public func compositionContent() -> AnyView { AnyView(ContentUnavailableViewContent(model: model)) }
  // composed, there is no window to wait for, so publication is what activates it.
  public func composeInto(_ parent: OneNativeCompositionParent) {
    controller?.detach(); controller = nil
    compositionParent = parent
    bindCallbacks()
    model.active = true
  }
  public func decompose() { compositionParent = nil; model.active = false }
  public override func didMoveToWindow() { super.didMoveToWindow(); updateHost() }
  public override func layoutSubviews() { super.layoutSubviews(); updateHost() }
  private func bindCallbacks() {
    model.onAction = { [weak self] id, actionCount in self?.onAction?(id, actionCount) }
  }
  private func updateHost() {
    guard compositionParent == nil else { return }
    model.active = false
    guard window != nil else { controller?.detach(); return }
    if controller == nil {
      bindCallbacks()
      controller = OneNativeHostingController(rootView: OneNativeStandalone(content: ContentUnavailableViewContent(model: model)))
    }
    controller?.attach(to: self)
    model.active = controller?.parent != nil
  }
  public func reset() {
    compositionParent = nil
    model.active = false; model.onAction = nil
    controller?.detach(); controller = nil; model = ContentUnavailableViewModel()
  }
}
private struct ContentUnavailableViewContent: View {
  @ObservedObject var model: ContentUnavailableViewModel
  var body: some View {
    ContentUnavailableView {
        if model.systemImage.isEmpty {
          Text(model.title)
        } else {
          Label(model.title, systemImage: model.systemImage)
        }
      } description: {
        if !model.description.isEmpty { Text(model.description) }
      } actions: {
        ForEach(model.actions, id: \.id) { action in
          Button(role: OneNativeGenerated.buttonRole(action.role), action: { model.action(action.id) }) {
            Text(action.label)
          }
        }
      }
      .oneNativeAccessibility(model.accessibility)
      .oneNativeStyle(model.swiftStyle)
  }
}
