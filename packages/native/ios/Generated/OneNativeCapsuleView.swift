// generated by bun run generate from the SwiftUI SDK and codegen/catalog.ts.
// edit the generator or catalog, then regenerate.
import SwiftUI
import UIKit

private final class CapsuleModel: ObservableObject {
  @Published var fill: UIColor? = nil
  @Published var stroke: UIColor? = nil
  @Published var lineWidth: Double = 1
  @Published var accessibility = OneNativeAccessibility()
  @Published var swiftStyle = OneNativeStyle()
  var active = false
}
@objcMembers public final class OneNativeCapsuleView: UIView, OneNativeComposable {
  private var model = CapsuleModel()
  public var onHeight: ((CGFloat) -> Void)?
  private var controller: OneNativeHostingController<OneNativeMeasuredStandalone<CapsuleContent>>?
  public override init(frame: CGRect) { super.init(frame: frame) }
  required init?(coder: NSCoder) { fatalError("init(coder:) is unavailable") }
  public func configureAccessibility(_ label: String, hint: String, value: String, identifier: String) {
    let next = OneNativeAccessibility(label: label, hint: hint, value: value, identifier: identifier)
    if model.accessibility != next { model.accessibility = next }
  }
  public func configureStyle(_ style: [String: Any]) {
    let next = OneNativeStyle(dictionary: style)
    if model.swiftStyle != next { model.swiftStyle = next }
  }
  public func configure(_ fill: UIColor?, stroke: UIColor?, lineWidth: Double) {
    if model.fill != fill { model.fill = fill }
    if model.stroke != stroke { model.stroke = stroke }
    if model.lineWidth != lineWidth { model.lineWidth = lineWidth }
  }


  private weak var compositionParent: OneNativeCompositionParent?
  public func compositionContent() -> AnyView { AnyView(CapsuleContent(model: model)) }
  // composed, activation follows the parent: a subtree mounted before root attachment
  // stays silent until the root attaches.
  public func composeInto(_ parent: OneNativeCompositionParent) {
    controller?.detach(); controller = nil
    compositionParent = parent
    bindCallbacks()
    model.active = parent.compositionActive
  }
  public func decompose() { compositionParent = nil; model.active = false }
  public func propagateActive(_ active: Bool) { model.active = active }
  public override func didMoveToWindow() { super.didMoveToWindow(); updateHost() }
  public override func layoutSubviews() { super.layoutSubviews(); updateHost() }
  private func bindCallbacks() {
  }
  private func updateHost() {
    guard compositionParent == nil else { return }
    model.active = false
    guard window != nil else { controller?.detach(); return }
    if controller == nil {
      bindCallbacks()
      controller = OneNativeHostingController(rootView: OneNativeMeasuredStandalone(content: CapsuleContent(model: model), onHeight: { [weak self] height in self?.onHeight?(height) }))
    }
    controller?.attach(to: self)
    model.active = controller?.parent != nil
  }
  public func reset() {
    compositionParent = nil
    model.active = false
    controller?.detach(); controller = nil; model = CapsuleModel()
  }
}
private struct CapsuleContent: View {
  @ObservedObject var model: CapsuleModel
  var body: some View {
    Group {
        if let fill = model.fill, let stroke = model.stroke {
          Capsule().fill(Color(uiColor: fill))
            .overlay(Capsule().stroke(Color(uiColor: stroke), lineWidth: model.lineWidth))
        } else if let fill = model.fill {
          Capsule().fill(Color(uiColor: fill))
        } else if let stroke = model.stroke {
          Capsule().stroke(Color(uiColor: stroke), lineWidth: model.lineWidth)
        } else {
          Capsule()
        }
      }
      .oneNativeAccessibility(model.accessibility, decorativeWhenUnlabeled: true)
      .oneNativeStyle(model.swiftStyle)
  }
}
