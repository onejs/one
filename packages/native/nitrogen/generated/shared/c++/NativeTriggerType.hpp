///
/// NativeTriggerType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::one {

  /**
   * An enum which can be represented as a JavaScript union (NativeTriggerType).
   */
  enum class NativeTriggerType {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    TIMEINTERVAL      SWIFT_NAME(timeinterval) = 1,
    DATE      SWIFT_NAME(date) = 2,
    PUSH      SWIFT_NAME(push) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::one

namespace margelo::nitro {

  // C++ NativeTriggerType <> JS NativeTriggerType (union)
  template <>
  struct JSIConverter<margelo::nitro::one::NativeTriggerType> final {
    static inline margelo::nitro::one::NativeTriggerType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"): return margelo::nitro::one::NativeTriggerType::UNKNOWN;
        case hashString("timeInterval"): return margelo::nitro::one::NativeTriggerType::TIMEINTERVAL;
        case hashString("date"): return margelo::nitro::one::NativeTriggerType::DATE;
        case hashString("push"): return margelo::nitro::one::NativeTriggerType::PUSH;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum NativeTriggerType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::one::NativeTriggerType arg) {
      switch (arg) {
        case margelo::nitro::one::NativeTriggerType::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::one::NativeTriggerType::TIMEINTERVAL: return JSIConverter<std::string>::toJSI(runtime, "timeInterval");
        case margelo::nitro::one::NativeTriggerType::DATE: return JSIConverter<std::string>::toJSI(runtime, "date");
        case margelo::nitro::one::NativeTriggerType::PUSH: return JSIConverter<std::string>::toJSI(runtime, "push");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert NativeTriggerType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"):
        case hashString("timeInterval"):
        case hashString("date"):
        case hashString("push"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
