// SSR page inside (app) route group
// This exists so that (app) is a valid route group with screens
export default function AppHomePage() {
  return (
    <div>
      <h1>App Home</h1>
      <p>This is the app home page inside the (app) route group.</p>
    </div>
  )
}
