{
  "name": "test-cloudflare",
  "version": "1.2.56",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "one dev",
    "build:web": "one build",
    "serve": "wrangler dev dist/worker.js --port 3457",
    "test": "vitest run"
  },
  "dependencies": {
    "one": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "playwright": "^1.42.1",
    "typescript": "^5.4.0",
    "vitest": "^4.0.6",
    "wrangler": "^4.0.0"
  }
}
