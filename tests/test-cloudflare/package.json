{
  "name": "test-cloudflare",
  "version": "1.16.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "one dev",
    "build:web": "one build",
    "serve": "wrangler dev --port 3457 --inspector-port 9237 --config dist/worker/wrangler.json",
    "test": "vitest --run --reporter=verbose"
  },
  "dependencies": {
    "one": "workspace:*"
  },
  "devDependencies": {
    "kill-my-port": "1.1.2",
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0",
    "wrangler": "4.60.0"
  }
}
