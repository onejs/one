{
  "name": "test-cloudflare",
  "version": "1.1.494",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "one dev",
    "dev:clean": "one dev --clean",
    "clean": "one clean",
    "build:web": "one build",
    "serve": "wrangler dev -c ./dist/wrangler.toml",
    "test-failing": "vitest --run"
  },
  "dependencies": {
    "one": "workspace:*",
    "react-native": "~0.79.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.38",
    "@cloudflare/workers-types": "^4.20241216.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.0",
    "wrangler": "^3.97.0"
  }
}
