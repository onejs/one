{
  "name": "one-tray",
  "version": "1.16.8-1778020391854",
  "private": true,
  "scripts": {
    "build": "[ \"$(uname)\" = \"Darwin\" ] && make debug || true",
    "release": "make release",
    "install-app": "make install",
    "clean": "make clean",
    "dev": "make debug && open OneTray.app",
    "tray": "pkill -f one-tray || true; make debug && open OneTray.app"
  }
}
